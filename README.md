# Student-Record-System
A menu-driven, terminal-based Student Record System built in Python. Supports adding, viewing, searching, updating, and deleting student records with persistent JSON storage, input validation, and error logging. Built with a modular architecture (data model, storage layer, and CLI kept separate) following clean coding practices.
